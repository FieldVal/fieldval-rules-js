FieldVal Rules works both in the browser as demonstrated above, and as a node package.

To use FieldVal Rules as a node package, install ```fieldval-rules``` using ```npm```:

```bash
npm install fieldval-rules --save
```